#include "main.h"

/**
* mul - give the multiple of 2 integers
* @a: first number to multiple by the second
* @b: second number to multiple by the first
* Return: the multiple of 2 integers
*/
int mul(int a, int b)
{
	return (a * b);
}
